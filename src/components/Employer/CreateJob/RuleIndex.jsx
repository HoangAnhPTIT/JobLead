import React from "react";

const RuleIndex = ({ index }) => {
	return (
		<div className="border-2 border-33 text-center font-bold rounded-full min-w-[30px] h-[30px]">
			{index}
		</div>
	);
};

export default RuleIndex;
